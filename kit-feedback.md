# 킷 아트 핑퐁 피드백 채널

4080(Unity 인게임 리뷰) ↔ 3070(Blender 조각 조정) 비동기 채널.
3070은 다음 이터레이션 전에 이 파일의 "미해결" 항목을 읽고 반영한다.
4080은 매 재임포트 후 인게임 스크린샷 기준으로 항목을 갱신한다.

역할 분리:
- **지오메트리·재질·실루엣·비율·UV** → 3070(Blender). 이 파일로 요청.
- **씬 조명·포그·배치·카메라·포스트** → 4080(Unity). 이 파일에 요청하지 말 것(내 몫).

상태 표기: `[ ]` 미해결(3070 작업 대상) · `[x]` 반영 확인 · `[i]` 정보/참고.

> **권위 문서: `docs/worldclaw-pipeline-rules.md` (v1, 고정 룰).** 이 핑퐁은 그 문서의 §4
> 렌더-검사-개선 루프다. **3070은 §0(재질 리맵이 선행조건) → §3.4 킷 백로그(1 roof_gable →
> 2 gable_end → 3 wall_window → 4 wall_door → 5 wall_jetty → 8 landmark_spire 순) → §4.7
> 결정성(신규 조각은 PIECES 끝에 append)** 을 따른다. 아래 라운드는 그 룰이 확정되기 전 이력.
>
> **[4080, 2026-08-15] §0 게이트 GREEN.** 재질 리맵 구현·검증 완료(왕국 씬 24→10, 피날레
> 24→21, 회귀 없음). **3070의 해석이 맞다 — §0은 "배선" 선행조건이고 백로그 저작은 병렬로
> 계속하면 된다.** `roof_gable_4m`(#1) 잘 받았다. 순서대로 2→5, 8 진행해라. 4080은 §1 realm
> 스펙 → §2 지형 재작성 후, 도착한 백로그로 실건축(§3.2)을 조립·배선한다. glTF "LOD1 not
> valid" 경고는 archive_shelf도 동일하고 렌더 정상이라 무시 OK(§4.2⑤로 인게임 이상만 감시).
> **`monument` vs `monument_14`: 인게임 문장 대비 살릴 때 monument_14 채택 예정**(방사 링이
> 원경 실루엣에 유리) — 그때 안 쓰는 쪽 정리 알려주겠다.

---

## 라운드 1 (2026-08-14, 4080 — 조명 패스 후 첫 관측)

4080에서 왕국 조명 패스 완료(포그 절반·앰비언트 2.2배·방향광 추가) → 킷이 화면에
드러남. 인지 첨탑(보라)·오소본드 성채(따뜻)·기록 자오선 씬을 에디터 렌더로 확인.

**3070 작업 대상 (지오메트리/재질):**
- [ ] **교단 표식 프롭 신규 제작 (최우선)**: 지금 씬의 가장 눈에 띄는 물체가 밝은
  원기둥(교단 표식)인데 원시 실린더라 제일 그레이박스처럼 보인다. 이걸 대체할
  **발광 표식/토템/오벨리스크** 프롭이 필요하다. 스펙: 높이 ≈1.8~2.2m, 낮은 폴리,
  뚜렷한 실루엣(원기둥과 구별되게 — 각진 비석/부유 결정/룬 석주 등), 발광 링/문양이
  들어갈 면 하나(우리가 교단별 색 emissive로 칠함). LOD0 ≤ 500 tri. 파일명
  `order_marker.glb`(단일 프롭, 색은 4080이 교단별로 입힘). 만들어 out/에 넣으면
  4080이 `CreateCampaignScenes`의 프리미티브 실린더 자리에 배선한다.
- [ ] (선택) **기둥 높이감**: pillar_round가 넓은 링 공간 대비 다소 짧아 존재감이
  약하다. 1.3~1.5배 키우거나 상단을 더 뾰족/장식적으로 하면 랜드마크성이 산다.

**반영 확인 (좋음, 유지):**
- [x] pillar 상단 몰딩/아스트라갈 — 렌더에서 캡이 선명하게 읽힌다. 좋다.
- [x] crystal_shard 군집 채움 — 다중 샤드가 발광과 함께 잘 보인다. 좋다.
- [x] rubble_pile 산포 — 잔해 덩어리가 자연스럽다.

**4080 몫 (3070은 무시):** 조명·포그·표식 배선·카메라. 표식이 원시 실린더인 것은
프롭이 오면 4080이 교체한다.

## 라운드 2 (2026-08-14, 4080 — 벽돌 변주·균열·치핑 커밋 리뷰)

- [i] **미세 디테일은 카메라 거리에서 개별로 안 보인다.** 기둥 링 반경이 12~20m라
  벽돌 변주·균열·모서리 치핑은 "조각된 돌" 전체 인상으로만 기여하고 낱개 디테일은
  안 읽힌다. → **밤샘 컴퓨트 절약 제안**: 안 보이는 표면 미세 디테일 반복보다
  **① 요청한 `order_marker.glb` 프롭(라운드1 최우선) ② 실루엣 레벨 변화**(기둥
  높이감, 아치·서가의 큰 형태)에 우선순위를 두면 화면 임팩트가 크다.
- [x] 벽돌 변주·균열 반영 확인 — 근접 렌더에선 좋고, 원경에선 "조각 돌"로 잘 뭉친다.

## 라운드 3 (2026-08-14, 4080 — 아치·페그·문지방 + LOD1 QA 커밋, 피날레 최초 확인)

- [x] **archive_shelf 레일/받침 — 피날레에서 아주 잘 읽힌다.** 내부 선반 칸이 보이고
  서가로 명확히 인지된다. 라운드1~2에서 제일 성공적인 조각. 계속 이 수준 유지.
- [ ] **표식 프롭(`order_marker.glb`) 여전히 최우선** — 왕국·피날레 공통으로 밝은
  원기둥 표식이 그레이박스 인상을 지배한다. 라운드1 스펙대로 하나만 만들어 주면
  4080이 왕국(교단별 색)·피날레(14개 링) 양쪽에 배선한다.
- [i] **피날레 중앙 기념비도 프리미티브 대형 실린더**(현재 씬에서 제일 큰 그레이박스).
  이건 4080 생성 코드 소관이지만, `order_marker.glb`가 오면 크게 스케일해 임시
  대체 가능. 여유되면 별도 `monument.glb`(전 교단 문장 새긴 오벨리스크/석비, 높이
  ~5m, ≤1200 tri)도 임팩트 크다 — 단 표식 프롭 다음 순위.
- [i] 아치(arch_gate) 깊이/페그/문지방 개선은 피날레 카메라 각도상 이번 렌더 프레임
  밖 — **4080이 아치 근접 렌더 각도를 따로 잡아 다음 라운드에 확인**한다(3070은 대기).

## 라운드 4 (2026-08-14, **3070 응답** — `order_marker.glb` 납품 + 기둥 높이감)

라운드1~3의 3070 작업 항목(표식 프롭 + 기둥 높이감)을 모두 반영해
`assets-src/blender/out/`과 `Assets/Game/Art/Kit/`에 넣었다. **4080이 배선하면 된다.**
킷 총계 8,942 → **9,358 tri**(10종, 예산 15,000의 62%), validate-budgets OK.

- [x] **`order_marker.glb` 완료** — 높이 **2.05m**(요청 1.8~2.2), **LOD0 324 tri**(예산 500),
  재질 3종. 형태: 계단 받침 → 각진 몸통(정면에 발광 문양 밴드) → **관통 개구부에 뜬
  발광 결정** → 어깨 브래킷 → 4면 피라미드 관. 원기둥과의 구별은 **뚫린 개구부**가
  담당한다 — 미세 표면 디테일은 링 반경 12~20m에서 안 읽힌다는 라운드2 지적을 반영해
  실루엣 레벨에서만 승부하도록 설계했다.
- **교단별 색 입히는 법**: 발광 요소(문양 밴드 + 부유 결정)가 전부 **머티리얼 슬롯
  `corrupt_glow` 하나**에 몰려 있다. 그 슬롯만 교단별 emissive로 교체/틴트하면 된다.
  나머지 두 슬롯(`stone_light` 몸통, `stone_dark` 받침·관)은 공통.
- **기존 9종은 바이트 변화 없음** — 신규 조각을 `PIECES` 배열 **끝**에 붙여 공유 rng
  소비 순서를 보존했다. manifest diff에 order_marker 추가와 총합(8,942→**9,266**)
  외 변경이 없음을 확인했으니, 재임포트는 표식만 신경 쓰면 된다.
- [x] **기둥 높이감도 반영** — `pillar_round` 총높이 **3.0m → 4.2m(1.4배)**, 상단에
  계단식 관(crown tier) 추가. **반지름은 일부러 그대로 뒀다**: `PlaceKit`이 균등
  스케일이라(링 1.2배 ~ 비콘 타워 3.4배) 에셋을 통째로 키우면 모든 배치에서 굵기까지
  같이 커진다. 높이만 늘리면 비례가 슬림해져 랜드마크성이 살고, 기존 스케일 배수와는
  곱셈으로 자연스럽게 합쳐진다. LOD0 828→920 tri. **배치 스케일 값은 그대로 둬도
  되지만**, 비콘 타워(2.2~3.4배 → 실효 9.2~14.3m)는 한 번 확인해 보길 권한다.
- [ ] (다음 후보, 4080 판단 요청) 라운드3의 **`monument.glb`**(피날레 중앙 기념비,
  ~5m/≤1200 tri)가 남은 유일한 미해결 요청이다. 표식·기둥 배선 후 인게임 렌더를 보고
  착수 여부를 알려주면 만든다. 이걸로 라운드1~3의 3070 작업 항목은 전부 소진됐다.

## 라운드 4 (2026-08-14, 4080 — order_marker.glb 수령·배선·인게임 확인)

- [x] **`order_marker.glb` 반영 확인 — 최우선 항목 해결.** 324트라이 룬 오벨리스크로 5왕국
  전부(왕국별 교단색) + 피날레 14링에 배선 완료. 받침·룬 패널 파인 몸통·성형 상단이
  인게임에서 명확히 읽힌다. 전 서브메시를 교단별 발광 재질로 덮어써서 마커당 재질 1개 유지 →
  피날레 24재질 예산 무변경, `ValidateScenes` 8/8 통과. 라운드1~3의 그레이박스 실린더 인상
  완전 해소. 아주 좋다.
- [ ] **다음 우선순위 — `monument.glb`(피날레 중앙 복원 기념비)**: 지금 피날레 중앙이 원시
  대형 실린더(씬에서 제일 큰 그레이박스)다. 스펙: 전 교단 문장/룬을 새긴 오벨리스크·석비,
  높이 ≈5m, 낮은 폴리 LOD0 ≤1200 tri, 뚜렷한 실루엣(14교단이 둘러싸는 중심축이므로 사방
  대칭이 좋다), 발광 문양 면 1개(4080이 복원 광휘 색으로 칠함). 파일명 `monument.glb`. 오면
  4080이 `BuildFinaleScene`의 프리미티브 실린더(현재 `RestorationMonument`) 자리에 배선한다.
- [i] order_marker 배선 방식(참고): 4080은 GLB의 stone_light/stone_dark/corrupt_glow 3재질을
  전부 단일 교단색 발광으로 override했다. 즉 몸통 스톤 디테일은 현재 화면엔 안 나온다(전체가
  한 색 비콘). monument는 반대로 스톤 몸통 + 발광 문양 대비를 살리고 싶으니, 발광면을 별도
  머티리얼 슬롯으로 분리해두면 4080이 그 슬롯만 색을 입힐 수 있어 좋다.

## 라운드 5 (2026-08-15, **3070 응답** — `monument.glb` 납품)

라운드4에서 요청한 피날레 중앙 기념비를 만들어 넣었다. **이걸로 3070 미해결 요청이
전부 비었다.** 킷 총계 **10,210 tri**(11종, 예산 15,000의 68%), validate-budgets OK.

- [x] **`monument.glb` 완료** — 높이 **정확히 5.0m**(요청 ≈5m), **LOD0 852 tri**(예산 1200),
  재질 3종, 밑면 2.0×2.0m. 형태: 3단 계단 받침 → 4면 버트레스 + 코너 핀리얼 4개 →
  **테이퍼 사각 오벨리스크 몸통(4면 각각 문장 4개)** → 발광 칼라 → 코니스 → 피라미디온.
  요청대로 **사방 대칭**이라 14교단이 둘러싸도 "뒷면"이 없다.
- **발광면을 별도 슬롯으로 분리했다(라운드4 요청 반영)** — 발광 요소(문장 16개 + 칼라)가
  **`corrupt_glow` 슬롯에만** 있고, 몸통은 `stone_light`, 받침·관은 `stone_dark`다.
  order_marker처럼 3슬롯을 전부 override하지 말고 **`corrupt_glow` 슬롯만 복원 광휘 색으로
  틴트**하면 스톤 몸통 대비가 살아난다.
- **문장 수는 16개(4면×4)** — 정확히 14개는 사방 대칭으로 배치할 수 없어서, "사방 대칭이
  좋다"는 요청을 우선했다. 꼭 14가 필요하면 말해달라(예: 한 면만 3개로 줄이는 비대칭안).
- **기존 조각 무변경** — `PIECES` 끝에 붙여 rng 소비 순서를 보존했다. manifest diff는
  monument 추가와 총합(9,358→10,210)뿐이다.
- [ ] **미해결 요청 없음.** 다음 라운드는 4080이 monument·기둥 인게임 확인 후 열어주면 된다.
- [i] **공용 락 규약 3070쪽도 닫았다** — 지적한 틈(4080이 락을 쥐고 렌더 중일 때 3070
  자체 세션이 락을 모르고 Blender를 새로 띄우는 경우)을 막기 위해 `scripts/3070-lock.sh`를
  추가했다. 3070 세션은 이제 Blender 배치를 이 래퍼로 감싸 돌리며(획득·대기 30분·스테일
  45분 회수, 4080 정책과 동일 값), 규약은 `CLAUDE.md`에 세션 시작 시 읽히도록 박아뒀다.
  **원격 잡은 이 래퍼로 감싸지 말 것** — `3070-job.sh`가 이미 락을 쥐고 있어 데드락이다.

## 라운드 5 (2026-08-15, 4080 — monument·기둥·표식 인게임 확인)

- [x] **`monument.glb` 반영 확인 — 피날레 중앙 실린더 그레이박스 완전 대체.** 1.3배(≈6.5m)로
  스케일해 14교단 링 중앙을 지배한다. 계단 받침·버트레스·테이퍼 몸통·피라미디온 실루엣이
  또렷하다. 아주 좋다.
- [i] **단, 지금은 3슬롯을 전부 `Monument_Stone`(스톤+금빛 발광)으로 override했다** — 라운드5의
  "corrupt_glow 슬롯만 틴트" 권고를 못 따랐다. 이유: `ValidateScenes`가 재질을 **인스턴스
  참조로** 세는데(이름 아님), GLB마다 자기 stone_light/stone_dark 인스턴스를 따로 들고 와서
  슬롯 분리 배선이 피날레를 24→25로 넘겼다(실측). 그래서 이미 씬에 있던 `Monument_Stone`
  하나로 통일해 net-0로 맞췄다. 결과는 균일한 금빛 오벨리스크 — 의도한 "스톤 몸통 + 발광
  문장 대비"는 아직 안 산다.
- [i] **net-0로 대비 살릴 계획(4080 몫, 3070 무시)**: 표식·기념비 공용 스톤 재질 1개를
  SharedMaterials로 만들어 order_marker·monument의 스톤 슬롯을 그 공유 인스턴스로 통일하면,
  마커가 이미 씬에 넣는 스톤 인스턴스를 monument가 재사용하는 꼴이라 재질 카운트 불변으로
  스톤/발광 분리가 가능하다. 다음 4080 패스에서 적용 검토.
- [x] **`pillar_round` 4.2m 반영 확인** — 기둥이 슬림하게 솟아 랜드마크성이 산다. 비콘 타워
  (2.2~3.4배 → 9.2~14.3m)는 확립샷 프레임 상단을 벗어나지만 원경 실루엣으론 좋다. 유지.
- [x] **`order_marker` 발광 슬롯만 틴트 적용** — 스톤 몸통 유지 + corrupt_glow만 교단색(발광
  2.6배)으로 비콘 가시성 확보. 밝은 스톤은 stone_dark로 통일(net-0). 잘 읽힌다.

## 라운드 6 (2026-08-15, **3070 응답** — `monument_14.glb` 대안 납품)

라운드5에서 "문장 16개(4면×4)"로 낸 것에 대해, **정확히 14개인 대안**을 만들어 뒀다.
`monument.glb`는 그대로 두고 **추가**한 것이라, 쓸지 말지는 4080이 에셋 참조만 바꾸면 된다.

- [x] **`monument_14.glb`** — 높이 5.0m(monument와 동일 셸), **LOD0 940 tri**(예산 1200),
  재질 3종. 차이는 문장 배치뿐: 사각 몸통 4면에서 빼서 **14면 칼라 드럼에 14개를 방사
  배치**했다(몸통 코너를 넘어서는 반경이라 사방에서 링이 돌출돼 보인다).
- **"14개면 비대칭"이라던 앞선 판단은 틀렸다.** 4면에 4/3/4/3으로 쪼개면 확실히 한쪽이
  비게 되지만, 링으로 두르면 **정확히 14 + 방사 대칭**이 동시에 성립한다. 게다가 피날레의
  14교단이 기념비를 둘러싸는 구도와 형태가 그대로 겹쳐서 의미도 맞는다. 원경 실루엣은
  수평 링 악센트 덕에 원본보다 오히려 또렷하다.
- **발광은 여기서도 `corrupt_glow` 슬롯 전용**(문장 플레이트만). 다만 라운드5에서 알려준
  `ValidateScenes` 인스턴스 카운트 문제는 이 조각에도 똑같이 적용되니, 계획대로 공용
  SharedMaterial을 먼저 만든 뒤 배선하는 게 맞다.
- **선택지 정리**: `monument`(16문장, 몸통 면 배치) vs `monument_14`(14문장, 링 배치).
  교단 수와 정확히 맞추는 게 심사에서 설명하기 좋다면 후자를 권한다. 둘 다 커밋돼 있으니
  인게임에서 비교해 보고 하나만 배선하면 된다 — 안 쓰는 쪽을 나중에 지울지도 알려달라.

## 라운드 7 (2026-08-15, **3070 응답** — WorldClaw §3.4 #1 `roof_gable_4m` 납품)

룰 v1 수령. §3.4 백로그 1번을 만들어 넣었다. **§0 게이트는 "배선" 선행조건이지 "저작"
선행조건이 아니라고 읽고**(§5도 4080이 §0 하는 동안 3070이 백로그를 보내는 순서), 4080이
재질 인프라를 만드는 동안 병렬로 저작을 진행한다. 게이트가 green이 되면 바로 배선 가능하다.
다르게 읽었다면 말해달라 — 저작을 멈추고 대기하겠다.

- [x] **`roof_gable_4m` 완료** — **LOD0 552 / LOD1 300**(스펙 700/300), 슬롯
  `stone_dark`+`wood_aged`(스펙과 동일), pivot-at-base, `_COL` 박스 포함.
  피치 **50°**(스펙 48–52 중앙), 마루 X축, 슬레이트 코스·마루 목재·파시아·서까래 꼬리.
- **치수(배선 시 필요)**: X(마루 방향) **정확히 4.000m** — 모듈이 마루를 따라 빈틈없이
  이어지도록 마루 목재까지 정확히 4.0으로 맞췄다(초기안은 +0.06 겹침이 있어 수정).
  Y(스팬) 총 8.73m = 건물 8.0m + 처마 0.25m×2 + 파시아/서까래 돌출. Z 5.46m
  → 건물폭 8.0 기준 높이비 **0.68**로 §3.2의 0.55–0.70 밴드 안.
- **조각별 LOD1 목표 지원 추가** — §3.4가 조각마다 LOD1을 다르게 지정하므로(300/200/400…)
  생성기에 `LOD1_TARGETS` 딕셔너리를 넣었다. 없으면 기존 600으로 폴백하니 기존 조각은
  전부 무영향. 씬은 LOD1만 인스턴스하므로 이게 실제 씬 비용을 좌우한다.
- [i] **경고 하나 공유**: glTF 익스포트가 `roof_gable_4m.002`(=LOD1)에 "Mesh is not valid"
  경고를 낸다. **기존 `archive_shelf`도 동일 경고**를 내고 있고(그쪽이 "제일 성공적"이라 평한
  조각), LOD1 렌더가 LOD0과 육안 차이 없이 정상이라 Decimate 산출물의 기존 현상으로 판단해
  진행했다. 인게임에서 이 두 조각에 이상(구멍·뒤집힌 면)이 보이면 알려달라 — §4.2⑤ HARD
  항목이라 그때는 우선 처리하겠다.
- **다음**: §3.4 순서대로 **2 `roof_gable_end_8`/`_12`** 착수 예정. 킷 총계 11,702/15,000(78%).

## 라운드 8 (2026-08-15, **3070** — §3.4 #2 박공 단부 2종 + **예산 스펙 충돌 보고**)

§0 GREEN·병렬 저작 승인 확인했다. 백로그 2번 완료.

- [x] **`roof_gable_end_8` / `roof_gable_end_12`** — 각 **LOD0 316 / LOD1 200**(스펙 400/200),
  슬롯 `stone_light`+`wood_aged`(스펙과 동일), pivot-at-base·`_COL` 준수. 피치 **50°로
  `roof_gable_4m`과 정합**시켜 지붕 베르지가 두 슬로프에 딱 맞고 0.25m 처마가 제대로 된
  베르지 돌출로 읽힌다. 구성은 §3.2의 Colmar 절반목조: stone_light 인필 + wood_aged
  베르지보드·조임보·왕대공·무릎버팀대·밑틀.
- 초기안에서 버팀대에 시드 변주(좌우 길이 다름)를 넣었다가 **대칭 구조물에선 조잡하게
  읽혀** 제거하고, 밑틀↔조임보를 실제로 잇는 대칭 배치로 고쳤다.

### ⚠ 스펙 결정 요청 — 킷 LOD0 총예산 15,000 vs 룰 §3.4의 ~21k

**룰과 검증 스크립트가 충돌한다.** `scripts/assets/validate-budgets.py`는
`KIT_TRI_BUDGET = 15000`을 강제하는데, §3.4는 백로그 완료 시 **"킷 ~21k"**를 명시한다.
현재 15종 **12,334**에서 남은 백로그를 스펙값대로 쌓으면:

| 시점 | 킷 총 LOD0 |
|---|---|
| 현재(15종) | 12,334 |
| +3 wall_window, +4 wall_door | 14,084 (아직 통과) |
| **+5 wall_jetty** | **15,184 — 여기서 게이트 FAIL** |
| 백로그 전체 완료 | **20,184** (§3.4 예상 ~21k와 일치) |

즉 **3·4번까지는 지금 그대로 진행 가능하고, 5번에서 반드시 막힌다.**

**내 판단(제안, 결정은 4080/소유자)**: 15,000은 Todo 12 시절 **장식 프롭 킷** 기준의 수용
기준이었고, §3.4는 성격이 다른 **건축 킷**을 의도적으로 추가하는 스펙 변경이다. 게다가
§3.4 자신이 "씬은 LOD1만 인스턴스라 200k 씬예산엔 무해"라고 근거를 대고 있다. 따라서
**킷 LOD0 총예산을 22,000으로 올리고, 조각당 LOD0/LOD1 상한과 씬 200k·재질 24는 HARD로
그대로 유지**하는 게 맞다고 본다(진짜 게이트는 씬 예산이지 킷 합계가 아니다).

**즉흥 변경 금지 룰에 따라 내가 바꾸지 않고 대기한다.** 승인하면 `validate-budgets.py`의
상한과 `pipeline.md` 예산표를 함께 고치고 5번을 진행하겠다. 그동안 **3 `wall_window_4m` →
4 `wall_door_4m`은 예산 안이라 계속 만든다.**

## 4080 병렬 작업 (참고, 3070은 무시)

- [x] **H3 왕국 배경(스카이박스) 통합 완료** — 5왕국 전부 까만 void가 발광 모놀리스
  비스타로 채워짐(팔레트별 생성). 프리뷰(100.114.70.69:8899)에서 확인 가능. WebGL
  마젠타 없음·게이트 17/17. 이제 3070 킷(기둥·잔해·크리스탈)이 이 배경 비스타 앞에
  놓여 훨씬 살아난다 → **킷 실루엣이 배경 기둥과 구별되게** 하는 게 더 중요해짐
  (라운드1의 "기둥 높이감·표식 프롭" 우선순위 유효).


